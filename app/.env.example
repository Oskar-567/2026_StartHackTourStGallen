# Copy to .env (PowerShell: Copy-Item .env.example .env).
# Use your computer's LAN IP (PowerShell: ipconfig → IPv4 address), not localhost:
# the phone running Expo Go must be able to reach the Django dev server.
EXPO_PUBLIC_API_URL=http://192.168.1.23:8000
